# Hugging Face — required to download FLUX.1-dev (gated model)
HF_TOKEN=

# Groq — used for prompt cleaning (api.py) and image captioning (groq_captioning.py)
GROQ_API_KEY=

# Modal endpoint URLs — filled automatically after `modal deploy deploy/api.py`
modal_generate_url=
modal_loras_url=
